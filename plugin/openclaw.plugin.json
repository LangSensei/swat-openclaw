{
  "id": "swat-mcp-bridge",
  "name": "SWAT MCP Bridge",
  "version": "1.1.0",
  "description": "Bridge between OpenClaw and SWAT Commander MCP server. Spawns SWAT as a long-lived stdio process and registers its tools as native OpenClaw tools.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "binaryPath": {
        "type": "string",
        "description": "Path to the SWAT binary"
      },
      "runtime": {
        "type": "string",
        "description": "Agent runtime (default: copilot)"
      }
    },
    "required": ["binaryPath"]
  }
}
